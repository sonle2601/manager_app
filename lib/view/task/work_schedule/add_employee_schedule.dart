import 'package:flutter/material.dart';

import '../../../widgets/app_text.dart';

class AddEmployeeSchedule extends StatelessWidget {
  const AddEmployeeSchedule({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
